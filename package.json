{
  "name": "personal-projects",
  "private": true,
  "description": "Monorepo root. Do not put next here — that makes Vercel run a Next.js build from the repo root. The app is insider-alpha/web; see ./vercel.json."
}
